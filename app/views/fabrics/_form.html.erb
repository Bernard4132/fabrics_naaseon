<%= form_for(fabric) do |f| %>
  <% if fabric.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fabric.errors.count, "error") %> prohibited this fabric from being saved:</h2>

      <ul>
      <% fabric.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :fabricimage %>
    <%= f.file_field :fabricimage %>
  </div>

  <div class="field">
    <%= f.label :price %>
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
